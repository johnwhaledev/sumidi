{
  "name": "sumidi",
  "version": "1.0.0",
  "description": "suMidi — generatore automatico di brani MIDI (basso, chitarra, piano, batteria, ensemble)",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write \"**/*.{js,html,css,md}\"",
    "format:check": "prettier --check \"**/*.{js,html,css,md}\""
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "prettier": "^3.3.3",
    "vitest": "^2.0.5"
  }
}
